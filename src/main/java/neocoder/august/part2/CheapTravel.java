package neocoder.august.part2;

import java.util.Scanner;
// https://codeforces.com/group/2ufDfj3KI0/contest/336450/problem/V
public class CheapTravel {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        long travelNum = scanner.nextLong();
        long travelNumPerPass = scanner.nextLong();
        long oneTicketPrice = scanner.nextLong();
        long onePassPrice = scanner.nextLong();
/*
        double min;
        n=in.nextInt();
        m=in.nextInt();
        a=in.nextInt();
        b=in.nextInt();
        int op1=(n*a);
        min=op1;
        double op2 =((n/m))*b;
        double temp=(double)n/m;
        if(Math.ceil(temp)!=temp)
            op2+=b;
        if(min>op2)
            min=op2;
        op1=0;
        op1+=((n/m)*b);
        op1+=(n%m)*a;
        if(op1<min)
            min=op1;
        System.out.println((int)min);
        in.close();

*/

    }
}
